<dashboard stylesheet="customStyle.css">
  <label>Locations</label>
  <row>
    <panel>
      <viz type="leaflet_maps_app.maps-plus">
        <search>
          <query>source="capture-*" | dedup dst_fqdn | table dst_fqdn, dst_latitude, dst_longitude | where dst_latitude != "" and dst_longitude != "" | rename dst_latitude as latitude, dst_longitude as longitude</query>
          <earliest>0</earliest>
          <latest></latest>
        </search>
        <option name="drilldown">none</option>
      </viz>
    </panel>
  </row>
</dashboard>
